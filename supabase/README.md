# Supabase CLI / Dashboard — linked project

This repo targets the Supabase project **Tom's Mysterious Restaurant Web** (`project_ref`: `fxnngojptcdlukqvfiov`).  
Local `.env` is generated from Dashboard API keys; do not commit `.env`.
